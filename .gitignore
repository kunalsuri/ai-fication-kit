node_modules/
__pycache__/
*.pyc
.DS_Store
.env
*.env
test/tmp-*/
CLAUDE.local.md
.claude/settings.local.json

# Regenerated by `verify`/`drift`; the JSON manifests embed a wall-clock timestamp,
# so tracking them dirties the tree on every run. The .md reports stay tracked.
ai/analysis/audit-reports/*_MANIFEST.json
