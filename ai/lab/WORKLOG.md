<!-- Copyright (c) 2026 Kunal Suri (CEA LIST). All rights reserved. -->

# Work ledger — ai-fication-kit

The repo's **episodic memory of work**: one row per unit of work (feature added,
bug fixed, refactor, process change), linking the spec that authorized it, the
decisions behind it, the review that checked it, the evaluation that scored it,
and the commits that shipped it. The maps in `ai/guide/` say what the repo *is*;
this ledger says *what was done to it, when, and under which contract*.

## Rules

- **Append-only.** New work gets a new row with the next `W-<n>` ID. Never delete
  or renumber rows; a rolled-back change gets Status `rolled-back`, not removal.
- **One row per unit of work** — the same unit the spec describes. A row without
  a spec link is a process violation, not a shortcut.
- **Backtick every artifact path, written from the repo root** (e.g.
  ai/lab/specs/SPEC_x.md, backticked). `verify` checks backticked paths against
  the file tree, so a row whose artifacts vanished fails CI instead of rotting
  silently. Use `—` for artifacts that genuinely don't apply (e.g. no ADR was
  needed).
- **Agents append rows tagged `[inferred]`** like everything else in `ai/`;
  the human flips them to `[verified]` when auditing. Never flip it yourself.
- **Status vocabulary:** `specced` → `in-progress` → `in-review` → `shipped`
  (or `rolled-back` / `dropped`; suffix ` (blocked-env)` when verification could
  not run in the working environment).
- **Type vocabulary:** `feature` · `bugfix` · `refactor` · `docs` · `process`.

## Ledger

<!-- Example row (copy, replace the angle-bracket fields, backtick real paths
     written from the repo root). The example ID W-000 is reserved for this
     comment — real rows start at W-001:
| W-000 | 2026-01-15 | feature | Short title | ai/lab/specs/SPEC_<name>.md | ai/lab/decisions/ADR_<n>-<t>.md | ai/lab/reviews/REVIEW_W-000.md | ai/lab/evaluations/EVAL_<name>.md | <commit/PR> | FEATURE_MAP row | shipped | [inferred] |
-->

| ID    | Date       | Type    | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Spec                                                                                                                                                            | ADRs | Review                                                                                                                      | Eval | Commits / PR                                                                 | Knowledge updated                                                                                                                                                                                                                                                                                                                                             | Status      | Provenance                         |
| ----- | ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------- |
| W-001 | 2026-07-03 | process | Engineering loop: fix-bug + review-change workflows, work ledger, verify coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `ai/lab/specs/SPEC_engineering-loop.md`                                                                                                                         | —    | — (waived: this work shipped the review process itself, so no fresh-context review existed to run — recorded per AUD-R3-08) | —    | PR #23, merge bafa3d0                                                        | `ai/lab/README.md`, `ai/INDEX.md`                                                                                                                                                                                                                                                                                                                             | shipped     | [verified] (05/07/2026 14:25 CEST) |
| W-002 | 2026-07-04 | bugfix  | Fix audit R3 defects AUD-R3-01..11 + AUD-R2-11 (fail-first regression tests, 365/365)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `ai/lab/specs/BUGFIX_audit-R3-fixes.md`                                                                                                                         | —    | `ai/lab/reviews/REVIEW_W-002.md` (approve) + Copilot PR #26 review (2 comments, addressed in 825b00f)                       | —    | 206e24e, 2b5d0b6, 825b00f (branch claude/deep-codebase-audit-zmd328, PR #26) | `ai/analysis/audit-reports/DEFECT_TRACEABILITY.md` rows flipped; `ai/guide/MODULE_MAP.md` baseline re-anchored to 825b00f; `ai/guide/FEATURE_MAP.md` gotchas added for orient/indepth/install/audit + stale ci-checks fix (8ca033a, review addendum approve)                                                                                                  | shipped     | [verified] (05/07/2026 14:25 CEST) |
| W-003 | 2026-07-04 | docs    | MCP KB server spec authored and hardened (freshness model, 3 adversarial audit rounds)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `ai/lab/specs/SPEC_mcp-kb-server.md`                                                                                                                            | —    | — (spec-stage work; hardened via `ai/analysis/audit-reports/ADVERSARIAL_AUDIT_2026-07-04_R3.md`)                            | —    | PR #27 + spec-fix commits in PR #28                                          | —                                                                                                                                                                                                                                                                                                                                                             | specced     | [verified] (05/07/2026 14:25 CEST) |
| W-004 | 2026-07-04 | process | /implement-spec command + engineering-loop docs (kit-repo dogfood only; promotion into templates/ deferred — maintainer's product decision, see `docs/IMPLEMENT-SPEC.md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | — (no spec — process gap found by the 2026-07-04 loop audit, recorded not retrofitted)                                                                          | —    | — (merged without fresh-context review — same audit finding)                                                                | —    | PR #28                                                                       | `docs/IMPLEMENT-SPEC.md`, README engineering-loop section, `docs/METHODOLOGY.md` §7                                                                                                                                                                                                                                                                           | shipped     | [inferred]                         |
| W-005 | 2026-07-04 | process | Engineering-loop audit + sync: ledger brought current, one canonical loop definition, always-on Record rule, add-feature WORKLOG lookup                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `ai/lab/specs/SPEC_engineering-loop-sync.md`                                                                                                                    | —    | — (review pending: /review-change in a fresh session)                                                                       | —    | branch claude/engineering-loop-audit-cmwfbd                                  | `ai/lab/README.md`, `docs/METHODOLOGY.md`, `docs/GLOSSARY.md`, CLAUDE.md/AGENTS.md + template twins                                                                                                                                                                                                                                                           | in-progress | [inferred]                         |
| W-006 | 2026-07-04 | feature | Close the 2026-07-04 online template re-audit: native `.claude/rules/` for Claude Code (always-on `ai-knowledge-layer.md` twin of the Cursor rule + path-scoped `provenance.md` `ai/**` guard), auto-memory clarifier, README context/harness/loop framing                                                                                                                                                                                                                                                                                                                                                                                                      | `ai/lab/specs/SPEC_claude-rules-parity.md`                                                                                                                      | —    | — (review pending: /review-change in a fresh session)                                                                       | —    | branch claude/docs-templates-review-e3kzn3                                   | `templates/claude/rules/ai-knowledge-layer.md`, `templates/claude/rules/provenance.md`, `.claude/rules/ai-knowledge-layer.md`, `.claude/rules/provenance.md`, `CLAUDE.md`, `AGENTS.md`, `test/run-tests.mjs`, `templates/README.md`, `README.md`, `docs/MULTI-TOOL-SETUP.md`, `CHECKSUMS.txt`, `ai/install-manifest.json`                                     | in-review   | [inferred]                         |
| W-007 | 2026-07-05 | process | Commands & skills overlap audit (strict-judge pass over all 11 commands + 5 skill copies across 4 integrations): 6 findings — P1 `/check-drift` target-repo defect, P1 command/skill double registration, doc + Step-0 drift — nothing removable outright                                                                                                                                                                                                                                                                                                                                                                                                       | — (audit, not a change; findings report is the deliverable)                                                                                                     | —    | —                                                                                                                           | —    | PR #31 (branch claude/commands-skills-audit-81oda2)                          | `ai/analysis/audit-reports/2026-07-05-commands-skills-audit.md`                                                                                                                                                                                                                                                                                               | in-review   | [inferred]                         |
| W-008 | 2026-07-05 | process | SPEC_TEMPLATE rewritten to the implementation-grade shape proven by the MCP KB spec (hard-constraints table, glossary-with-pitfalls, exact-behaviour section, Stability column in touch list, numbered test plan, definition-of-done) with scale-down guidance so small features stay lightweight; groundwork for the /write-spec → /implement-spec loop                                                                                                                                                                                                                                                                                                        | — (template change; the consolidation spec will follow)                                                                                                         | —    | —                                                                                                                           | —    | PR #31 (branch claude/commands-skills-audit-81oda2)                          | `templates/ai/lab/specs/SPEC_TEMPLATE.md`, `ai/lab/specs/SPEC_TEMPLATE.md`                                                                                                                                                                                                                                                                                    | in-review   | [inferred]                         |
| W-009 | 2026-07-05 | docs    | README + docs staleness sync: the workflow-command roster lagged the code (10 universal commands now, not 8 — `/fix-bug` and `/review-change` were missing) and only the `add-feature` skill was named where `fix-bug` also ships; `docs/README.md` hub said "all 8" CLI commands vs 12 in `docs/CLI-REFERENCE.md`; README "What You Get" tree was missing `ai/START-HERE.html`, `ai/lab/reviews/`, and `ai/lab/WORKLOG.md`; the `docs/reports/technical-report-draft.md` academic draft had zero coverage of the engineering loop — bumped to revision v6 (10-command roster, `fix-bug` skill, work ledger, new §9.5 on the loop + kit-only `/implement-spec`) | — (surgical doc drift fix, no code change)                                                                                                                      | —    | —                                                                                                                           | —    | branch claude/update-readme-docs-7glmi7                                      | `README.md`, `docs/README.md`, `docs/FAQ.md`, `docs/reports/technical-report-draft.md`                                                                                                                                                                                                                                                                        | in-review   | [inferred]                         |
| W-010 | 2026-07-05 | feature | New LOCAL-ONLY `/check-docs` command: a maintainer doc-drift audit that checks the README + docs/ against the real command/skill/CLI roster (source of truth: `templates/` + `install.mjs`), the stamped `ai/` tree, cross-doc count consistency, and backtick path-claims — read-only findings report, no auto-edits. Grew out of the W-009 session's staleness. Kept out of `templates/` (never distributed), following the `/implement-spec` precedent; made the mechanism explicit in `.claude/LOCAL-ONLY-COMMANDS.md`                                                                                                                                      | — (small kit-local command; the LOCAL-ONLY-COMMANDS.md note is the design record)                                                                               | —    | — (review pending: /review-change in a fresh session)                                                                       | —    | branch claude/update-readme-docs-7glmi7                                      | .claude/commands/check-docs.md, `.claude/LOCAL-ONLY-COMMANDS.md`                                                                                                                                                                                                                                                                                            | in-review   | [inferred]                         |
| W-011 | 2026-07-05 | feature | Shazam update mode + `update` command: deterministic updater for already-installed repos — version awareness (`firstInstalled` + `history` in the install manifest), CHANGELOG-parsed what-changed digest, pre/postflight health bracket, obsolete-file detection (deletion behind a real interactive prompt only; `--yes` never unlocks it), declarative rename registry for future template moves, downgrade guard                                                                                                                                                                                                                                            | — (design agreed in-session with the maintainer: `CHANGELOG.md` as the notes source, always-ask deletion, auto-detect + `update` alias, headline-bullet digest) | —    | — (review pending: /review-change in a fresh session)                                                                       | —    | branch claude/shazam-update-verification-oq7ahk                              | `lib/update.mjs` (new), `lib/migrations.mjs` (new), `lib/installer.mjs`, `install.mjs`, `test/run-tests.mjs`, `docs/CLI-REFERENCE.md`, `CHANGELOG.md`; PR #34 review addressed (manifest path sanitization, anchored semver regex, CodeQL escape finding); `ai/guide/MODULE_MAP.md` drift baseline re-anchored to d6b06c0 (covers main PRs #31–#33 + this PR) | in-review   | [inferred]                         |
| W-012 | 2026-07-05 | process | Feature-lifecycle tracking: consolidated the two docs/dev/features-* files into one `ai/lab/ROADMAP.md` (planned backlog + shipped index, cousin of WORKLOG); relocated the personas/simulation narrative to lessons-learnt; registered in INDEX/CLAUDE/AGENTS + docs hub; repaired inbound links; deleted the old docs/dev/features-upcoming-implemented folder (content migrated). Command-matrix wiring (/add-feature, /implement-spec) and CI-enforced trace (add ROADMAP to verify sources) deferred per session decision                                                                                                                                  | `ai/lab/specs/SPEC_feature-lifecycle-tracking.md`                                                                                                               | —    | — (review pending: /review-change in a fresh session)                                                                       | —    | (uncommitted — working tree on main)                                         | `ai/lab/ROADMAP.md`, `docs/dev/lessons-learnt/feature-planning-personas-and-simulation.md`, `ai/INDEX.md`, `CLAUDE.md`, `AGENTS.md`, `docs/README.md`, `docs/dev/lessons-learnt/model-tiering-plan-heavy-implement-light.md`                                                                                                                                  | in-progress | [inferred]                         |
| W-013 | 2026-07-05 | feature | Roadmap lifecycle wiring: `/add-feature` + `/implement-spec` now move the feature ROADMAP row Planned to Shipped (14 tool copies); `verify` scans `ai/lab/ROADMAP.md` with a verify-ignore fence for forward-looking specs and backticked Shipped spec links; adopter `templates/ai/lab/ROADMAP.md.tmpl` stamped + registered; deep-test placeholder check skips backtick/fenced spans (clears ROADMAP/FEATURE_MAP/SPEC_B5 false positives) | `ai/lab/specs/SPEC_feature-lifecycle-tracking.md` | — | — (review pending: /review-change in a fresh session) | — | branch claude/roadmap-lifecycle-wiring | `lib/verify.mjs`, `ai/lab/ROADMAP.md`, `templates/ai/lab/ROADMAP.md.tmpl`, `templates/ai/INDEX.md.tmpl`, `templates/ai/lab/README.md.tmpl`, `ai/lab/README.md`, `test/run-tests.mjs`, `test/run-deep-test.mjs` | in-review | [inferred] |
| W-014 | 2026-07-05 | process | Dev-loop template audit: confirmed `/implement-spec` absent from all 4 template surfaces (deliberate W-004 deferral, not accident); every other loop stage verified mechanism-backed in `templates/`; live new-user + update-mode simulations run against a scratch repo; promotion path proven (update delivers a promoted command automatically, no migration) and promotion blockers recorded (kit-only `install.mjs` invocation, `SPEC_engineering-loop.md` reference) | — (audit, not a change; findings report is the deliverable) | — | — | — | branch claude/dev-loop-template-check-kvh6uq | `ai/analysis/audit-reports/2026-07-05-dev-loop-template-audit.md` | in-review | [inferred] |
| W-015 | 2026-07-05 | process | Local-only registry gap fix: `.claude/LOCAL-ONLY-COMMANDS.md` tracked only slash commands, silently omitting the `deep-test` skill (in `.agents/skills/` only, no `templates/` twin, no `.claude/skills/` mirror — same exclusion class as `/implement-spec` but undocumented, found via a full local-vs-templates diff across all four tool surfaces requested during PR #35 review) | — (doc-registry fix; the diff audit is the design record) | — | — | — | branch claude/dev-loop-template-check-kvh6uq | `.claude/LOCAL-ONLY-COMMANDS.md` | in-review | [inferred] |
| W-016 | 2026-07-05 | docs | Big-picture Excalidraw diagrams for onboarding: three hand-editable scenes in `docs/diagrams/` answering "what is this codebase" (system map: deterministic/inference/human producers → the `ai/` layer → readers), "how did we get here" (v0.1.0 → now, the AI-native → spec+loop mindset shift, sourced from `CHANGELOG.md`), and "what is the daily loop" (Spec→Decide→Implement→Review→Evaluate→Record). Emitted by a deterministic zero-dep generator that also renders `.svg` previews so they show in GitHub; wired into the `docs/README.md` hub's reserved `docs/diagrams/` slot | — (docs deliverable; framing drawn from `docs/METHODOLOGY.md` + `ai/guide/`) | — | — (review pending: /review-change in a fresh session) | — | branch claude/codebase-architecture-diagrams-5vukcm | `docs/diagrams/generate.mjs`, `docs/diagrams/README.md`, `docs/diagrams/01-system-map.excalidraw`, `docs/diagrams/02-evolution-journey.excalidraw`, `docs/diagrams/03-engineering-loop.excalidraw`, `docs/README.md` | in-review | [inferred] |
| W-017 | 2026-07-05 | bugfix | Fixed GitHub CodeQL alert #1 (js/incomplete-sanitization, CWE-20/80/116): `test/release-check.mjs`'s version-to-regex escape only handled dots, not backslashes or other regex metacharacters; replaced with a full regex-escape; re-ran `node test/release-check.mjs` to confirm all gates still pass | — (single-line security fix, no spec) | — | — (fix pending fresh-context review) | — | PR #39 (branch fix/codeql-incomplete-sanitization) | `test/release-check.mjs` | in-review | [inferred] |
| W-018 | 2026-07-05 | bugfix | Fixed four defects surfaced by a real-world new-user simulation (the kit run end-to-end against the external OSS repo debug-js/debug): (1) `doctor` reported an unaudited cold-start map as "Step 5 — trusted", contradicting `status`'s "NEEDS AUDIT" — it counted only literal `[inferred]` rows, so a map whose rows ended in Stability `?` with no per-row tag fell through to trusted; now any row not `[verified]` (including untagged rows) holds it at step 3 in `lib/doctor.mjs`. (2) `verify` mis-read backticked API/method references shaped like name.ext (util.inspect, array.map) as missing files and failed `--strict`; added a closed known-file-extension allow-list in `lib/verify.mjs` (path claims with a slash are unaffected). (3) `drift` reported existing files placed in the MODULE_MAP Directory column as "vanished", contradicting `verify`'s "confirmed"; "vanished" in `lib/drift.mjs` now means truly absent (neither dir nor file). (4) `shazam`/`install` under `--yes` or a non-interactive shell silently skipped the intake wizard's branch-safety guard; `install.mjs` now prints a non-blocking warning when installing onto main/master with the wizard skipped (reads the .git/HEAD file directly, no git shelled out). Each fix has a regression test; suite is 405/406 (the single failure is a pre-existing sandbox npm-pack environment error, confirmed present on clean main). | — (fixes from an in-session simulation findings report; no separate spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/suspicious-mcclintock-9134c6 | `lib/doctor.mjs`, `lib/verify.mjs`, `lib/drift.mjs`, `install.mjs`, `test/run-tests.mjs` | in-review | [inferred] |
| W-019 | 2026-07-05 | bugfix | PR #40 review follow-up: `verify`'s bare-filename allow-list omitted supported marker/build extensions (go.mod, *.sln, *.csproj, plus sibling .NET project types), so those backticked filename claims were skipped instead of being checked under `verify --strict`; the deny-list comment also overstated why entries like `console.log` are needed. Added the missing extensions in `lib/verify.mjs`, clarified the comment, and extended the regression test to prove API refs stay ignored while supported bare filenames are confirmed. | — (review-only follow-up on W-018; no new spec) | — | Copilot PR #40 review `4632045319` | — | branch claude/suspicious-mcclintock-9134c6 | `lib/verify.mjs`, `test/run-tests.mjs` | in-review | [inferred] |
| W-020 | 2026-07-06 | process | Full-stack real-repo simulation: ran the entire methodology (shazam indepth → /cold-start with repo-explorer → simulated audit → verify/drift/status/maturity → /add-feature with spec SPEC_F1 → fresh-context /review-change → ROADMAP/WORKLOG bookkeeping) against a fresh clone of fastapi/full-stack-fastapi-template (React/TS + FastAPI/Python, uv + bun workspaces). Feature F1 (version endpoint + sidebar display) shipped in the sandbox with a 10-file surgical diff; review verdict request-changes caught 2 real majors, fixed and re-verified 172/172. 10 kit findings recorded, severity-ranked: orient's wrong polyglot build/test commands stamped into 4 files (HIGH), 4-col stamped MODULE_MAP template vs 5-col parser/status expectation → "0 [inferred]" on fresh cold-starts (HIGH), top-level-only testDir/lockfile/src-dir detection (uv.lock + bun.lock unknown to maturity), indepth 0-dependency count on workspaces, README-bullet description scraping, cold-start↔no-churn rule conflict over fixing stamped commands, no blocked-env verification vocabulary, verify false-positives on @scope/pkg + repo slugs + pytest selectors, unconditional license-header rule, W-1 vs W-001 template inconsistency + missing `generated` stability idiom | — (simulation run; findings report is the deliverable) | — | — (target-side review: REVIEW_W-001 in the scratch repo) | — | branch claude/ai-native-repo-simulation-j7l2jb | `ai/analysis/audit-reports/2026-07-06-fullstack-simulation-report.md` | in-review | [inferred] |
| W-021 | 2026-07-06 | bugfix | PR #41 review follow-up (Copilot): `doctor` embedded the machine-absolute target path into its suggested commands, and `refreshProgressPage` bakes that output into the committed `ai/START-HERE.html` — the same leak 417e105 hand-restored on PR #39 without fixing the producer; `diagnose()` now renders the target relative to the caller's cwd (`.` from the repo root). Second half: the gitignored `*_MANIFEST.json` files made doctor claim "drift has never been run" on every fresh clone even when the committed `ai/analysis/audit-reports/DRIFT_REPORT.md` proves it ran; diagnosis now says "not been run on this checkout (committed report found — re-run to refresh)" when the report twin exists. Two regression tests added (relative-path action, fresh-clone wording); suite 408/408. START-HERE.html regenerated to an accurate step-5 state; PR body W-018→W-020 reference corrected | — (review-only follow-up; the PR comments are the record) | — | Copilot PR #41 review comments (discussion_r3526559543, r3526559569) | — | PR #41 (branch claude/ai-native-repo-simulation-j7l2jb) | `lib/doctor.mjs`, `test/run-tests.mjs`, `ai/START-HERE.html` | in-review | [inferred] |
| W-022 | 2026-07-06 | docs | Restructured the root `START-HERE.html` landing page into two clearly separated, tabbed tracks to end beginner/advanced confusion: a **Basic** track (3-step scaffold → `/cold-start` → audit + `verify`, plus a plain-language explainer of the three-kinds-of-knowledge trust model and Stability) and an **Advanced** track (a full util-belt table — `doctor`/`status`/`verify`/`drift`/`orient`/`indepth`/`check-repo-maturity`/`audit`/`shazam`/`update`/`demo`/`uninstall` with what+why each — and a dedicated section on why `ai/lab/ROADMAP.md`, `ai/lab/specs/` and `ai/lab/WORKLOG.md` exist and how the `ID → spec → work-unit → PR` trace links them). Facts sourced from `docs/CLI-REFERENCE.md`, `docs/METHODOLOGY.md`, and `ai/lab/ROADMAP.md`; vanilla dependency-free JS track switcher; existing dark theme/banner/copy-buttons preserved; the generated `ai/START-HERE.html` progress page is untouched. `verify --strict` clean (290/290). | — (surgical docs/UX change, no code path) | — | — (review pending: /review-change in a fresh session) | — | branch claude/start-here-docs-structure-ness1d | `START-HERE.html` | in-review | [inferred] |
| W-023 | 2026-07-06 | process | Implementation plan for the W-020 simulation findings, written for a lighter implementation model (/implement-spec contract, zero design decisions): `ai/lab/specs/SPEC_C9-detection-polyglot.md` (findings F1/F3/F4/F5/F8 — six work packages with exact line anchors into `lib/orient.mjs`, `lib/indepth.mjs`, `lib/maturity.mjs`, `lib/verify.mjs`, shared listWorkspaceMembers helper in `lib/util.mjs`, tests T1–T8) and `ai/lab/specs/SPEC_C10-template-alignment.md` (findings F2/F6/F7/F9/F10 — five work packages across the stamped templates and all 8 cold-start tool-surface copies, copy-parity tests T1–T3). ROADMAP: C9/C10 Planned rows + detail blocks + sequencing notes (C9 subsumes C4's first slice; C10 before C1). Specs are Status draft — human flips to approved before implementation starts | `ai/lab/specs/SPEC_C9-detection-polyglot.md`, `ai/lab/specs/SPEC_C10-template-alignment.md` | — | — (planning artifact; reviewed when implemented) | — | PR #41 (branch claude/ai-native-repo-simulation-j7l2jb) | `ai/lab/ROADMAP.md`, `ai/START-HERE.html`, `ai/analysis/audit-reports/VERIFICATION_REPORT.md` | specced | [inferred] |
| W-024 | 2026-07-06 | docs | Lessons-learnt write-up of the full-stack simulation session (companion to the W-020 findings report and the W-023 specs): seven lessons — vary the target repo shape per simulation run; template↔parser round-trip tests; committed artifacts must not embed machine-local state (fix the producer); a sanctioned BLOCKED-ENV verification vocabulary; the fresh-context review catches what the implementer cannot see; findings reports vs /implement-spec-grade specs as different artifacts for different audiences; small process frictions (churn-rule exception, ID-format drift, the generated-code frozen idiom). Linked from the docs hub | — (docs deliverable; sources are `ai/analysis/audit-reports/2026-07-06-fullstack-simulation-report.md` and the W-020/W-022 rows) | — | — | — | PR #43 (branch claude/ai-native-repo-simulation-j7l2jb) | `docs/dev/lessons-learnt/full-stack-simulation-session.md`, `docs/README.md` | in-review | [inferred] |
| W-025 | 2026-07-06 | process | CI security floor for the kit repo itself (this repo has zero runtime/dev dependencies, so nothing here changes what's audited today — it's a guard against a dependency landing unnoticed later): added `.github/dependabot.yml` (npm + github-actions ecosystems, weekly), a `.github/workflows/codeql.yml` (javascript-typescript, push/PR/weekly cron), and an `npm audit --audit-level=high` step in the existing `ai-check.yml` job right after its `npm ci`. Verified `npm audit --audit-level=high` passes clean (0 vulnerabilities) and the new YAML parses; `verify --strict` clean (302/302 claims). The 9 pre-existing MODULE_MAP `stale` drift rows (unrelated other-PR churn awaiting human re-audit) are untouched by this change | — (small, additive CI-config change; no spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/security-features-audit-ibdiub | `.github/dependabot.yml`, `.github/workflows/codeql.yml`, `.github/workflows/ai-check.yml` | in-review | [inferred] |
| W-026 | 2026-07-06 | process | Follow-up CI supply-chain hardening: pinned every third-party Action to its resolved commit SHA (with a `# vX.Y.Z` comment) across all 5 existing workflows (`test.yml`, `ai-check.yml`, `release.yml`, `release-check.yml`, `codeql.yml`), closing the "a moved tag silently changes what CI runs" gap — SHAs resolved via `git ls-remote --tags` against each upstream repo, not guessed. Added `.github/workflows/scorecard.yml` (OpenSSF Scorecard, weekly + push-to-main, uploads SARIF to code scanning) and `.github/CODEOWNERS` requiring review on `install.mjs`, `lib/`, and `.github/` (the code that runs on users' machines and the CI trust boundary itself). Branch-protection enforcement (require these checks + a review before merge to `main`) was recommended but explicitly deferred — it's a repo Settings change with no API tool available in this session, not a code change | — (small, additive CI-config change; no spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/security-features-audit-ibdiub | `.github/workflows/scorecard.yml`, `.github/CODEOWNERS`, `.github/workflows/test.yml`, `.github/workflows/ai-check.yml`, `.github/workflows/release.yml`, `.github/workflows/release-check.yml`, `.github/workflows/codeql.yml` | in-review | [inferred] |
| W-027 | 2026-07-06 | process | Cleared the 5 open Dependabot PRs the github-actions ecosystem entry from W-025 started generating (SHA bumps for actions/setup-node, actions/upload-artifact, github/codeql-action/upload-sarif, github/codeql-action/analyze, ossf/scorecard-action). Verified each PR touched only `.github/workflows/*.yml` (no app code, no `ai/` docs) before merging: confirmed `test`/`coverage` passed on all 5, and that the two checks that did fail (ai-check's drift gate, CodeQL analyze) were pre-existing failures already present on `main` (the same 9 stale `ai/guide/MODULE_MAP.md` rows from W-026's still-open backlog), not regressions from the bumps. Squash-merged PR #49, #48, #47, #46, #45 in that order with no conflicts | — (routine dependency-update triage; no spec) | — | — (mechanical SHA-bump merges; no design decision to review) | — | PRs #45, #46, #47, #48, #49 | `.github/workflows/ai-check.yml`, `.github/workflows/release-check.yml`, `.github/workflows/release.yml`, `.github/workflows/test.yml`, `.github/workflows/scorecard.yml`, `.github/workflows/codeql.yml` | done | [inferred] |
| W-028 | 2026-07-06 | process | v0.3.0 release prep: audited all 130 commits / W-004–W-027 since the `v0.2.0` tag against `CHANGELOG.md`'s `[Unreleased]` section and found it stopped short of `/implement-spec`, `/check-docs`, `/adversarial-audit`, the CI security-hardening pass (Dependabot, CodeQL, SHA-pinned Actions, OpenSSF Scorecard, CODEOWNERS), the START-HERE Basic/Advanced split, onboarding diagrams, and six bugfixes (W-017–W-019, W-021). Backfilled those entries, retitled the section `## [0.3.0] — 2026-07-06`, and bumped the version in every location `release-check` enforces (`package.json`, `lib/util.mjs` `KIT_VERSION`, `CITATION.cff`, README BibTeX). `npm run release-check` all-green, `npm test` 408/408, `verify --strict` 317/317 confirmed. Git tag / GitHub Release / Zenodo deposit deliberately left to the maintainer per `docs/RELEASE-CHECKLIST.md` — not run in this session | — (release-prep bookkeeping; no spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/ai-fication-kit-changelog-8e2njd | `CHANGELOG.md`, `package.json`, `lib/util.mjs`, `CITATION.cff`, `README.md` | in-review | [inferred] |
| W-029 | 2026-07-06 | process | Release-day checklist pass for v0.3.0 (`docs/RELEASE-CHECKLIST.md`, "Before tagging" section): hygiene sweep found no stray TODOs/placeholders; regenerated `CHECKSUMS.txt` (106 files, was stale since before the `/adversarial-audit` addition); dogfooding step (`node install.mjs install .`) caught that this repo's own install had never been re-run since `/adversarial-audit` was added to `templates/` — stamped the 4 missing files (.claude/commands/, `.agents/workflows/`, `.cursor/rules/`, `.github/prompts/`), refreshed `ai/install-manifest.json` (kitVersion 0.3.0, history row). `npm test` 408/408, `verify --strict` 320/320, `release-check` all-green after the dogfood re-run. Checked CI on `main`'s tip (8657eba): `CI (3 OSes)` and `OpenSSF Scorecard` green; `AI Knowledge-Base Check` red on the pre-existing 9 `stale` MODULE_MAP drift rows already tracked in W-025/W-026 (needs a human `/audit` re-verify pass, not a code fix); `CodeQL` red for a repo Settings conflict — "CodeQL analyses from advanced configurations cannot be processed when the default setup is enabled" — needs the maintainer to disable GitHub's Default Setup for code scanning (Settings → Code security) since the committed `codeql.yml` is the Advanced setup; no code or workflow change can fix this from inside the repo. Neither red check blocks a Zenodo release (Zenodo only requires the GitHub Release, not green CI) | — (release-day checklist execution; no spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/ai-fication-kit-changelog-8e2njd | `CHECKSUMS.txt`, .claude/commands/adversarial-audit.md, `.agents/workflows/adversarial-audit.md`, `.cursor/rules/adversarial-audit.mdc`, `.github/prompts/adversarial-audit.prompt.md`, `ai/install-manifest.json`, `ai/analysis/README.md` | in-review | [inferred] |
| W-030 | 2026-07-06 | process | Post-Settings-fix CI/Scorecard triage: maintainer disabled GitHub's Code Scanning Default Setup (the W-029 blocker); re-ran GitHub Actions run 28815326794 and confirmed `CodeQL` now completes (`conclusion: success`). That unblocked Scorecard's SARIF upload, which surfaced 7 findings in the repo's Code Scanning tab — clarified for the maintainer that these are OpenSSF Scorecard supply-chain practice checks (`Branch-Protection`, `Code-Review`, `Maintained` — High; `SAST`, `Fuzzing` — Medium; `CI-Tests` — Low), not CodeQL code vulnerabilities. Looked up Scorecard's actual detection logic (ossf/scorecard's checks/raw/fuzzing.go, not a path in this repo): `Branch-Protection`/`Code-Review` need a GitHub Settings change (require PR review + status checks on `main`) that only the maintainer can make; `Maintained` needs no action; `SAST`/`CI-Tests` should self-resolve as Scorecard re-runs against the now-fixed CodeQL and existing `test.yml` PR triggers; `Fuzzing` for JS/TS is detected *only* via a `fast-check` import — no custom harness registers. Flagged that adding `fast-check` would be this repo's first-ever npm dependency (devDependency-only, never shipped — `package.json` today has zero `dependencies`/`devDependencies`, a stated hard constraint in `ai/lab/ROADMAP.md`'s engineering contract). Maintainer chose to skip Fuzzing and keep the zero-dependency stance; no code changed | — (CI triage + a scope decision; no spec) | — | — | — | branch claude/ai-fication-kit-changelog-8e2njd | — (no files touched; decision recorded here) | done | [inferred] |
| W-031 | 2026-07-10 | feature | Promoted the cold-start and review-change workflows to distributed Agent Skills in both flavors — `.claude/skills/` (subagent-delegating: `repo-explorer` for the cold-start explore step, `test-runner` for the review-change suite re-run) and `.agents/skills/` (inline, tool-agnostic), each with a pre-completion checklist — authored under `templates/` so every kit-installed repo receives them alongside the existing add-feature/fix-bug skills, and dogfooded byte-identical into the repo. Regenerated `CHECKSUMS.txt` (106 to 114 files) and synced the skill roster across `README.md`, `docs/FAQ.md`, and `docs/MULTI-TOOL-SETUP.md` (also fixed a pre-existing staleness that named only the add-feature skill where fix-bug also ships). Scope was set interactively as the "core author loop" batch; implement-spec and the deep-test skill were deliberately NOT promoted — both are registered local-only in `.claude/LOCAL-ONLY-COMMANDS.md`, and implement-spec's promotion into `templates/` is an explicit deferred maintainer product decision (`docs/IMPLEMENT-SPEC.md`), so distributing them was left to the maintainer. `ai/install-manifest.json` refresh left to the maintainer's next `node install.mjs install .` dogfood pass (W-029 pattern; a dry-run showed a real run would also rewrite `ai/repo-profile.json`, unwanted churn on a skills-only branch). `npm test` 408/408, `verify --strict` clean; deep-test unchanged at 4/5 (the 9 pre-existing stale MODULE_MAP drift rows from W-025–W-029 are untouched by this change) | — (user-driven skill addition; scope set interactively — cold-start + review-change promoted, implement-spec + deep-test held per `.claude/LOCAL-ONLY-COMMANDS.md`) | — | `ai/lab/reviews/REVIEW_W-032.md` (approve; covers W-031+W-032, PR #56) | — | branch claude/project-skills-audit-1oq7kv | `templates/claude/skills/cold-start/`, `templates/agents/skills/cold-start/`, `templates/claude/skills/review-change/`, `templates/agents/skills/review-change/`, `.claude/skills/cold-start/`, `.agents/skills/cold-start/`, `.claude/skills/review-change/`, `.agents/skills/review-change/` (each with SKILL.md + a pre-completion checklist), `CHECKSUMS.txt`, `README.md`, `docs/FAQ.md`, `docs/MULTI-TOOL-SETUP.md` | in-review | [inferred] |
| W-032 | 2026-07-11 | refactor | Migrated the Claude Code surface from slash commands to skills, following the Claude Code v2.1.3 change that merged custom commands into skills (verified at code.claude.com/docs/en/skills — the agentskills.io open standard: a skill and a same-named command both create /name, and the skill wins). Converted all 13 .claude/commands/*.md to skills with bodies verbatim: the 4 lifecycle skills (add-feature, fix-bug, cold-start, review-change) already existed; the 7 distributed diagnostics (adversarial-audit, check-drift, create-feature-catalog, perform-feature-add-simulation, post-cold-start-verification, review-agent-config, verify-ai-readiness) and 2 local-only (implement-spec, check-docs) gained name + disable-model-invocation:true so they stay explicit /name workflows (implement-spec also got argument-hint). Deleted templates/claude/commands/ and .claude/commands/; the generic claude→.claude installer mapping stamps `templates/claude/skills/` with no installer code change. Added an 11-row command→skill rename registry to `lib/migrations.mjs` so existing installs are pointed at each successor on update. Cursor/Copilot/Antigravity surfaces deliberately untouched (the merge is Claude Code-specific). Updated tests (installed-file assertions, mapping test, safe-path example), the ai/ knowledge layer (`ai/guide/MODULE_MAP.md` row re-pointed and downgraded to [inferred] for human re-audit; `ai/guide/FEATURE_MAP.md` check-drift/adversarial-audit entries; `ai/analysis/FEATURE_CATALOG.md` F10 rows), and living docs (`README.md`, `templates/README.md`, `docs/FAQ.md`, `docs/MULTI-TOOL-SETUP.md`, `docs/GETTING-STARTED.md`, `START-HERE.html`, `docs/IMPLEMENT-SPEC.md`, `.claude/LOCAL-ONLY-COMMANDS.md`, the `ai-knowledge-layer` rules under `.claude/rules/`, `templates/claude/rules/`, `.cursor/rules/`, `templates/cursor/rules/`, and the `.claude/skills/check-docs/` auditor logic). Historical WORKLOG rows W-010/W-029 referencing removed command paths were un-backticked (W-012 precedent) to stay verify-clean without rewriting release history. All 26 SKILL.md conform to the standard (name↔dir, description ≤1536, no frontmatter angle brackets, <500 lines); npm test 408/408, verify --strict clean; deep-test unchanged at 4/5 (the 8 pre-existing stale MODULE_MAP drift rows are a human re-audit item). The 0.4.0 version bump + release is left to the maintainer (rename rows tagged sinceVersion 0.4.0; CHANGELOG [Unreleased] entry added) | — (user-directed migration grounded in the authoritative Claude Code skills docs; scope limited to the Claude surface — Cursor/Copilot/Antigravity out of scope by design) | — | `ai/lab/reviews/REVIEW_W-032.md` (approve; 2 minor/nit findings, no blockers) | — | branch claude/project-skills-audit-1oq7kv | `templates/claude/skills/`, `.claude/skills/implement-spec/`, `.claude/skills/check-docs/`, `lib/migrations.mjs`, `test/run-tests.mjs`, `CHECKSUMS.txt`, `ai/guide/MODULE_MAP.md`, `ai/guide/FEATURE_MAP.md`, `ai/analysis/FEATURE_CATALOG.md`, `README.md`, `templates/README.md`, `docs/FAQ.md`, `docs/MULTI-TOOL-SETUP.md`, `docs/GETTING-STARTED.md`, `docs/IMPLEMENT-SPEC.md`, `START-HERE.html`, `.claude/LOCAL-ONLY-COMMANDS.md`, `.claude/rules/ai-knowledge-layer.md`, `templates/claude/rules/ai-knowledge-layer.md`, `.cursor/rules/ai-knowledge-layer.mdc`, `templates/cursor/rules/ai-knowledge-layer.mdc`, `CHANGELOG.md` | in-review | [inferred] |
| W-033 | 2026-07-11 | process | Actioned both findings from `ai/lab/reviews/REVIEW_W-032.md`. Finding 1 (`ai/install-manifest.json` staleness): ran the dogfood `node install.mjs install . --yes` pass the review recommended, refreshing the manifest's file list and hashes to the post-migration `.claude/skills/*/SKILL.md` / `.agents/skills/*/SKILL.md` layout (the old `.claude/commands/*.md` entries are gone); `ai/repo-profile.json` was unchanged so no extra churn landed. Finding 2 (`ai/guide/MODULE_MAP.md` line 31 staleness): corrected the `templates/agents/` row's description from "the add-feature skill" (singular, stale since `fix-bug` landed pre-PR and `cold-start`/`review-change` landed in W-031) to name all four shipped skills (add-feature, fix-bug, cold-start, review-change), downgrading the row `[verified]` → `[inferred]` per the provenance rule since its content changed under an agent's hand. `npm test` regenerated the real repo's `ai/analysis/audit-reports/DRIFT_REPORT.md` as an unrelated side effect of its self-check `drift` invocation (which runs without `--git`, always reporting 0 stale) — reverted that file to keep the diff scoped to the two findings. `npm test` 408/408, `verify --strict` 345/345 confirmed. Pre-merge, ran the full `npm run deep-test`: 4/5 (unchanged category from W-025 onward — the drift `--strict` gate against pre-existing stale `[verified]` MODULE_MAP rows, a human re-audit item, not a code defect); count dropped 9 → 7 as a direct effect of this row's finding-2 fix (the `templates/agents/` row's downgrade to `[inferred]` removes it, and the corrected description also cleared `templates/claude/`'s stale flag). License-header and placeholder checks both pass; `ai/analysis/audit-reports/DRIFT_REPORT.md` (committed) and its gitignored DRIFT_MANIFEST.json twin, plus `ai/START-HERE.html`, refreshed to the real `--git` result | — (review follow-up; no new spec) | — | `ai/lab/reviews/REVIEW_W-032.md` findings 1 and 2 (this row is their resolution) | — | branch claude/review-w-032-findings-hgu58b | `ai/install-manifest.json`, `ai/guide/MODULE_MAP.md`, `ai/START-HERE.html` | done | [inferred] |
| W-034 | 2026-07-11 | test | Extended `test/run-tests.mjs` off a coverage-gap analysis (`npm run coverage`, c8/V8): 13 new checks for `lib/indepth.mjs`'s repo-health heuristics (missing `.gitignore`/LICENSE/`.env.example`/ARCHITECTURE.md/linter-config warnings and recommendations, both the trip and the non-trip side of each), 12 for `install.mjs` CLI edge cases (`--version`/`-v`, `--analysis-level` value validation, `orient --indepth` and standalone `indepth --dry-run` / corrupt-on-disk-profile tolerance, `shazam --indepth`, and the "None yet" primary-tool next-steps text), and 3 for `reserveBackupPath`'s collision-increment loop in `lib/util.mjs`. Raised aggregate coverage 88.05%→89.38% lines / 80.12%→81.6% branch (`install.mjs` 90.4%→98.66%, `lib/indepth.mjs` 81.57%→83.47%); 434/434 checks pass. Deliberately left untouched: `lib/intake.mjs` (49.73%) and `lib/audit.mjs` (62.7%) stay low because their interactive bodies (`runFirstRunWizard`, `audit()`) gate on `isInteractive()`/`readline`, which the sandboxed test runner has no TTY to satisfy — closing that gap needs a PTY-driven test lane, scoped as a separate follow-up, not attempted here. Two incidental self-check side effects (`ai/START-HERE.html` timestamp, `ai/analysis/audit-reports/DRIFT_REPORT.md` regenerated without `--git`) were reverted before commit, matching the W-033 precedent | — (analysis-driven test addition; no spec) | — | — (review pending: /review-change in a fresh session) | — | branch claude/test-coverage-analysis-soyb9t | `test/run-tests.mjs` | in-review | [inferred] |
| W-035 | 2026-07-11 | process | 360° research review of the kit against the July-2026 landscape (GitHub Spec Kit, AWS Kiro, Tessl, IBM Bob, the ETH Zurich AGENTS.md evaluation arXiv 2602.11988, model-routing and agent-memory literature), answering four maintainer questions: SDD-backbone fit (yes — the kit is the substrate the branded tools lack; gaps: ceremony ladder, /implement-spec not distributed, requirement-level traceability), repo-intelligence value on complex codebases (plausible but unproven — the published evidence contests context-file benefit on in-distribution repos, and the kit's own live A/B eval Part B is still unrun; a 3-arm experiment is specified), small-model enablement (strongest emergent property — plan-heavy/implement-light is field consensus and the kit's constraint surface makes it safe; needs implement-spec promotion, a deterministic speclint gate, a model-tier ledger column), and post-SDD future-proofing (the semantic/episodic/procedural memory split + provenance + deterministic gates are process-agnostic; MCP KB server and lab-artifact front-matter are the two structural bets). Names the three-planes framing (Map / Loop / Stamp) for the "many things at once" confusion; prioritized recommendations R1–R11 mapped onto roadmap items C3/C4/C6/C9 | — (research review; the evaluation document is the deliverable) | — | — | `ai/lab/evaluations/2026-07-11-360-review-sdd-fit-and-directions.md` | branch claude/aification-kit-review-4wvn3h | — | done | [inferred] |
| W-036 | 2026-07-11 | process | Drafted the implementation-grade spec for the deterministic spec-lint gate recommended by the W-035 360° review (§4, recommendation R3): a new speclint CLI command that mechanically decides whether a lab spec is ready to hand to a light-model implementer — status line, all eight template sections, hard-constraints table, explicit Out list, touch list with recognized Stability values cross-checked by position against `ai/guide/MODULE_MAP.md` (any disagreement is a transcription error and fails), frozen/? rows demanding a recorded human approval, modify-paths probed on disk under a containment rule, T-numbered test plan, numbered acceptance criteria, knowledge-update checklist, and zero unresolved template placeholders — reusing the existing `--strict` flag to promote warnings; read-only, stdlib-only, exit codes 0/1. The spec is written to the `docs/IMPLEMENT-SPEC.md` implementation-grade bar so a Sonnet-class model can execute it via /implement-spec with no design decisions left open: 14 checks (SL1–SL14) with exact named regexes, contractual output strings, and a 14-row test plan with verbatim fixtures for `test/run-tests.mjs`. Registered as roadmap item C11. Implementation deliberately not started in this unit: the human flips the spec Status draft → approved first, then a light-model session implements | `ai/lab/specs/SPEC_C11-speclint.md` | — | — (spec-stage work, W-003 precedent; the implementation unit runs the /review-change gate) | — | branch claude/sonnet-5-spec-doc-3nf3xh | `ai/lab/ROADMAP.md` (C11 tracking row + one-breath line) | specced | [inferred] |
| W-037 | 2026-07-11 | process | Registered the W-035 360° review's remaining recommendations as trackable roadmap backlog rows so the tracking table in `ai/lab/ROADMAP.md` is the single pending-work dashboard: C12 (R1 3-arm live A/B eval), C13 (R2 /implement-spec template promotion), C14 (R4 model-tier ledger column), C15 (R5 ceremony ladder), C16 (R6 requirement→test→commit traceability), C17 (R7 three-planes README restructure), C18 (R8 spec lifecycle vocabulary), C19 (R9 MCP knowledge-base server, linked to its existing `ai/lab/specs/SPEC_mcp-kb-server.md` as spec drafted), C20 (R10 lab-artifact YAML front-matter), C21 (R11 blast-radius auditing + verification-by-usage queue). Priorities and efforts copied from the review's §7 table (`ai/lab/evaluations/2026-07-11-360-review-sdd-fit-and-directions.md`); every row cites its R-number so the review → backlog mapping is followable in both directions; one-breath end-user value lines added per the existing C-row convention. All rows enter at status idea except C19 (spec drafted); R3 was already registered as C11 in W-036 | — (backlog bookkeeping; no spec) | — | — | — | branch claude/sonnet-5-spec-doc-3nf3xh | `ai/lab/ROADMAP.md` (C12–C21 tracking rows + one-breath lines) | done | [inferred] |
| W-038 | 2026-07-11 | feature | Implemented C10 (Knowledge-template & workflow alignment) per SPEC_C10-template-alignment.md Revision 2: W1 stamped `templates/ai/guide/MODULE_MAP.md.tmpl` to 5 columns (Directory/Responsibility/Entry point/Stability guess/Status) with a verify-ignored worked example, and extended the "Fill MODULE_MAP" step in all 5 cold-start body-text families (`.claude/skills/`, `.agents/skills/`, `.agents/workflows/`, `.cursor/rules/`, `.github/prompts/`, 10 files incl. `templates/` twins) with the FIVE-cell/Status-is-last-cell sentence. W2 added a new "Correct the stamped commands" cold-start step (same 10 files) plus the matching No-Churn exception clause (tagged `[inferred]`, scoped to Build/Test + CONVENTIONS.md Definition-of-done lines) in `templates/CLAUDE.md.tmpl`/`templates/AGENTS.md.tmpl`. W3 documented the generated/vendored `frozen` idiom in the template's Stability legend and `docs/AUDIT-GUIDE.md`. W4 softened the unconditional license-header rule to "match neighboring files" in `templates/AGENTS.md.tmpl` and both `SPEC_TEMPLATE.md` copies. W5 added the `BLOCKED-ENV` vocabulary to both `REVIEW_TEMPLATE.md` copies, the `(blocked-env)` WORKLOG status suffix to both `WORKLOG.md`/`.tmpl` rules blocks, and fixed the `W-1`→`W-001` example in `templates/ai/lab/ROADMAP.md.tmpl`. Pre-implementation: a general-purpose subagent audited SPEC_C9/SPEC_C10 (the maintainer has no bandwidth for a human spec audit) and found the touch list stale post-W-032/W-033 (commands→skills migration: 5 body-text families/10 files, not the spec's "4 twin pairs/8 files") and a gap in the proposed No-Churn exception wording (missing `[inferred]` tag + CONVENTIONS.md scope) — both folded into the spec as Revision 2 before implementing (see W-Correct commit). Added T1–T3 to `test/run-tests.mjs` per spec §6; found and fixed a real bug the new T1/T2 tests caught: the worked-example row's backticked illustrative paths (src/api/, src/api/main.ts) were being scanned by `verify` as real path claims on every fresh install, breaking `verify --strict` — fenced with the verify-ignore comment markers (bare marker form; a first attempt with trailing comment text silently failed to match the fence regex, caught by re-running the suite). 444/444 `npm test`, `verify --strict` 355/355, `deep-test` 4/5 unchanged (pre-existing MODULE_MAP stale-row backlog from W-025 on, not a regression). Generated-artifact side effects (`ai/START-HERE.html`, `ai/analysis/audit-reports/DRIFT_REPORT.md`) reverted before commit, W-033 precedent | `ai/lab/specs/SPEC_C10-template-alignment.md` | — | — (review pending: /review-change in a fresh session) | — | branch claude/roadmap-status-update-pzuepv | `.claude/skills/cold-start/SKILL.md`, `.agents/skills/cold-start/SKILL.md`, `.agents/workflows/cold-start.md`, `.cursor/rules/cold-start.mdc`, `.github/prompts/cold-start.prompt.md` (+ 5 `templates/` twins), `templates/CLAUDE.md.tmpl`, `templates/AGENTS.md.tmpl`, `templates/ai/guide/MODULE_MAP.md.tmpl`, `templates/ai/lab/specs/SPEC_TEMPLATE.md`, `ai/lab/specs/SPEC_TEMPLATE.md`, `templates/ai/lab/reviews/REVIEW_TEMPLATE.md`, `ai/lab/reviews/REVIEW_TEMPLATE.md`, `templates/ai/lab/WORKLOG.md.tmpl`, `ai/lab/WORKLOG.md` (rules block), `templates/ai/lab/ROADMAP.md.tmpl`, `docs/AUDIT-GUIDE.md`, `test/run-tests.mjs`, `ai/lab/ROADMAP.md` (C10 row Planned → Shipped) | shipped | [inferred] |
| W-039 | 2026-07-11 | feature | Implemented C9 (Detection-layer robustness on polyglot/monorepo targets) per SPEC_C9-detection-polyglot.md Revision 2, all six work packages: W1 `lib/orient.mjs` now treats bun.lock as equivalent to bun.lockb, adds uv.lock detection, and prefers uv over Poetry/Pipenv when the pyproject/requirements marker fires. W2 added a one-level workspace-member scan (new shared `listWorkspaceMembers()` export in `lib/util.mjs`) that clones the marker table per member, resolves package-manager lock flags and package.json scripts PER DETECTOR DIRECTORY (root vs. each member, so members no longer silently inherit the root's package manager), prefixes a build system's composed command with `cd <dir> && ` only when every detector sharing that buildSystem came from the same non-root member, and scans TEST_DIR_CANDIDATES inside each recognized member directory too. W3 detectDescription() now skips markdown list items on the first prose pass and, if nothing else qualifies, normalizes the first list item (strips the bullet, flattens `[text](url)` links, drops emphasis markers) as a fallback. W4 added PEP-621-style pyproject.toml dependency parsing (`dependencies = [...]` under `[project]` as production, `[dependency-groups]`/`[project.optional-dependencies]` entries as development — distinct from the existing Poetry-table parser) to both the root scan and a new workspace-member summation pass in `lib/indepth.mjs`. W5 `lib/maturity.mjs` added bun.lock/uv.lock to the recognized lock list, scans one level into workspace members for `src`/`lib`/`app`/etc. code directories, and the printed report now shows an "ai/ layer already installed" panel line (new `aiLayerInstalled` field on the `checkMaturity()` result) instead of a stale "Process N will run" block when `ai/repo-profile.json` already exists. W6 `lib/verify.mjs`'s extractClaims() now skips npm scoped-package tokens (@scope/pkg) and strips pytest node-ID selectors (file.py::test_name) before the existing `:line` suffix strip, so both resolve to real path claims instead of false "missing" reports. Updated `docs/CLI-REFERENCE.md`'s `orient` section (was root-only; now describes the one-level member scan and per-member package-manager scoping) per the spec's conditional §5/§8 item. Added T1–T8 to `test/run-tests.mjs` per spec §6, each verified against a hand-built fixture matching the spec's exact scenario before being committed to the suite (manual `node install.mjs orient/indepth/verify/check-repo-maturity` runs against `/tmp` fixtures, not just the assertions). 457/457 `npm test`, `verify --strict` 370/370, `deep-test` 4/5 unchanged (same pre-existing MODULE_MAP stale-row backlog as W-038, not a regression). ROADMAP: C9 row moved Planned → Shipped; its Planned detail block removed (spec is now the detail of record); C4 rescoped to its remaining slices (per-package MODULE_MAP sections, workspace-aware `drift`) now that C9 has shipped its first slice. Generated-artifact side effects (`ai/START-HERE.html`, `ai/analysis/audit-reports/DRIFT_REPORT.md`, `VERIFICATION_REPORT.md`) reverted before commit, W-033 precedent | `ai/lab/specs/SPEC_C9-detection-polyglot.md` | — | — (review pending: /review-change in a fresh session) | — | branch claude/roadmap-status-update-pzuepv | `lib/orient.mjs`, `lib/indepth.mjs`, `lib/maturity.mjs`, `lib/verify.mjs`, `lib/util.mjs` (new `listWorkspaceMembers` export), `docs/CLI-REFERENCE.md`, `test/run-tests.mjs`, `ai/lab/ROADMAP.md` (C9 row Planned → Shipped, C4 rescoped) | shipped | [inferred] |
| W-040 | 2026-07-11 | feature | Implemented C1 (Stack-aware agent instructions, kill the `package.json` false positive) per SPEC_C1-stack-aware-instructions.md Revision 2: exported `lib/orient.mjs`'s `DETECTORS` marker table (one word) and added a new `resolveConfigFiles(targetAbs)` helper in `lib/installer.mjs` that checks which of DETECTORS's plain-filename markers actually exist at the target root, backticking each hit (or falling back to the plain-text phrase "the project's build manifests" — no backticks, so `verify` raises no claim — when nothing matched). `placeholders(profile)` became `async placeholders(profile, targetAbs)` and its one call site in `install()` now awaits it. Replaced the literal backticked `package.json` in the "No Phantom Bugs & Configuration Churn" rule sentence with `{{CONFIG_FILES}}` in both `templates/CLAUDE.md.tmpl` and `templates/AGENTS.md.tmpl` — the kit's own hand-maintained root CLAUDE.md/AGENTS.md are untouched by design (this feature only affects what gets stamped into OTHER repos). Spec-audited by a general-purpose subagent before implementation (standing in for the maintainer's unavailable review bandwidth) with verdict APPROVE WITH CHANGES: confirmed every load-bearing claim (DETECTORS shape/line, placeholders' single call site, targetAbs already in scope, no other template contains the literal `{{CONFIG_FILES}}` string, verify's backtick-only claim extraction) checked out against the real code; flagged the Set-based marker dedup as harmless defensive coding worth a clarifying comment (not a design change) and a stale ROADMAP line-number estimate the spec itself never repeated. Added T1-T4 to `test/run-tests.mjs` per spec §6, each manually verified against a hand-built `/tmp` fixture (Python-only, JS, polyglot npm+Maven, no-marker) before being committed to the suite. 462/462 `npm test`, `verify --strict` 371/371 clean. Also fixed an unrelated pre-existing regression this session's own C10 work introduced: PR #62's Windows CI failed T3 (copy-parity) because `.gitattributes` forces `eol=lf` on `templates/**` but not on the live `.claude/`/`.agents/`/`.cursor/`/`.github/` cold-start copies, so a Windows checkout CRLF-converts the live files while their template twins stay LF — normalized line endings in the T3 comparison (separate commit, pushed ahead of this row). The pre-existing `ai-check` drift `--strict` failure (7 stale MODULE_MAP rows, confirmed failing on `main` itself across its last 5 runs, unrelated to this session's changes) was reported to the maintainer rather than self-fixed — flipping `[verified]` rows requires the human-only interactive `audit` command, which agents cannot invoke non-interactively by design. ROADMAP: C1 row moved Planned → Shipped, its Planned detail block removed (spec is now the detail of record), sequencing notes updated | `ai/lab/specs/SPEC_C1-stack-aware-instructions.md` | — | — (review pending: /review-change in a fresh session) | — | branch claude/roadmap-status-update-pzuepv, PR #62 | `lib/orient.mjs`, `lib/installer.mjs`, `templates/CLAUDE.md.tmpl`, `templates/AGENTS.md.tmpl`, `test/run-tests.mjs`, `ai/lab/ROADMAP.md` (C1 row Planned → Shipped) | shipped | [inferred] |
| W-041 | 2026-07-11 | feature | Implemented C2 (Stage-aware `status` verdict — no more "DRIFTING" on day one) per `SPEC_C2-status-verdict.md`: `computeStatus()` in `lib/status.mjs` now calls `diagnose()` (imported, unmodified, from `lib/doctor.mjs`) before its own DRIFTING/NEEDS AUDIT/TRUSTED logic and short-circuits to two new orientation verdicts — doctor stage 1 (no `ai/repo-profile.json`) → `NOT INSTALLED` (grey badge, "Next: shazam"); doctor stage 2 (`MODULE_MAP.md` missing or still the scaffolded `<fill in>` template) → `NOT MAPPED YET` (blue badge, "Next: /cold-start", plus an "expected at this stage" note ahead of the still-printed row/claim/drift counts). Doctor stages 3-5 keep the pre-C2 logic byte-identical. Added `style.blue` to `lib/util.mjs`'s terminal palette and two CSS classes + a JS branch to `templates/ai/START-HERE.html.tmpl` so the living progress page renders the new verdicts distinctly instead of falling into the amber "needs-audit" catch-all. Updated the `status` verdict table in `docs/CLI-REFERENCE.md`. Test suite: added 3 new fixtures (`status-no-map` repurposed to assert `NOT INSTALLED`; new `status-not-mapped-yet-absent` and `status-not-mapped-yet-template` fixtures for the two ways doctor stage 2 fires) and added a real `ai/repo-profile.json` to every pre-existing stage-3-5 fixture that lacked one (`status-drifting`, `status-drifting-drift`, `status-needs-audit`, `status-stale-audit`, `status-trusted`, the real-git fixture, `status-json`, `progress-refresh`) — without that file those fixtures would now read as day-one (`NOT INSTALLED`) instead of exercising DRIFTING/NEEDS AUDIT/TRUSTED, since none of them previously included a profile file. 468/468 `npm test`, `verify --strict` 376/376 clean. Manually exercised both new verdicts end-to-end against throwaway `/tmp` fixtures (no `ai/` at all → `NOT INSTALLED` + shazam next-step; `ai/repo-profile.json` only → `NOT MAPPED YET` + /cold-start next-step + the "expected at this stage" line). ROADMAP: C2 row moved Planned → Shipped, its Planned detail block removed (spec is now the detail of record), sequencing notes updated | `ai/lab/specs/SPEC_C2-status-verdict.md` | — | — (review pending: /review-change in a fresh session) | — | branch claude/ai-lab-roadmap-review-xv82jl | `lib/status.mjs`, `lib/util.mjs`, `templates/ai/START-HERE.html.tmpl`, `docs/CLI-REFERENCE.md`, `CHANGELOG.md`, `test/run-tests.mjs`, `ai/guide/FEATURE_MAP.md`, `ai/lab/ROADMAP.md` (C2 row Planned → Shipped) | shipped | [inferred] |
| W-042 | 2026-07-11 | bugfix | PR #63 CI fix: two pre-existing `main`-branch failures unrelated to C2, surfaced by the PR's checks. (1) `.github/workflows/codeql.yml`'s `init` step was still pinned to github/codeql-action@v3.36.3 while `analyze` had been bumped to v4.37.0 by a prior Dependabot PR (#55) that only bumped one of the two steps — CodeQL errored "Loaded a configuration file for version '3.36.3', but running version '4.37.0'". Repinned `init` to the same commit/tag as `analyze` (`99df26d4f13ea111d4ec1a7dddef6063f76b97e9` / v4.37.0) since both steps ship from the same `codeql-action` release and must match. (2) `ai-check`'s `drift --git --strict` reported 7 stale rows (`/`, `lib/`, `templates/`, `templates/ai/`, `templates/github/`, `test/`, `docs/`) against the `ai/guide/MODULE_MAP.md` anchor commit `d915943` (2026-07-05) — confirmed pre-existing on `main` itself (same 7 rows, same failure, verified via a clean clone of origin/main), not caused by C2. Re-anchored the `Last verified: ... @ commit` header line to this PR's tip (`7ee4e93`, the CodeQL-fix commit) following the exact AUD-R2-11 precedent set by PR #37 (2026-07-05 row): only the mechanical git-drift-tracking anchor moved, no `[verified]` row content or per-row audit date was touched or self-flipped — those stay the authoritative human signatures. `drift --git --strict` now reports 0 stale locally. 468/468 `npm test`, `verify --strict` 383/383 clean. Generated-artifact side effects (`ai/START-HERE.html`, `ai/analysis/audit-reports/DRIFT_REPORT.md`, `VERIFICATION_REPORT.md`) reverted before commit, W-033 precedent | — | — | — (CI-fix follow-up on an already-reviewed PR; /review-change deferred to the PR's own review) | — | branch claude/ai-lab-roadmap-review-xv82jl, PR #63 | `.github/workflows/codeql.yml`, `ai/guide/MODULE_MAP.md` (anchor re-stamp only) | shipped | [inferred] |
| W-043 | 2026-07-12 | docs | Technical-report reviewability restructure: added a front-matter Executive Summary to `docs/reports/technical-report-draft.md` — a P1–P5 purpose map (context/memory for agents, harness engineering, human-verified intelligence, mechanical honesty over time, human onboarding) with per-purpose section pointers, plus five reader-intent reading paths — and grouped the Table of Contents into five thematic parts. Section numbering, anchors, and body content unchanged (surgical, additive-only); fixed the metadata table's stale Report Revision field (said v5 while the footer recorded v6; now v7). 468/468 `npm test` (incl. docs link integrity), `verify --strict` 387/387 clean; generated-artifact side effects (`ai/START-HERE.html`, drift/verification reports) reverted before commit per W-033/W-042 precedent | — (docs-structure change requested directly by the maintainer; no code touched) | — | — | — | branch claude/technical-report-structure-sg8e0n | `docs/reports/technical-report-draft.md` | shipped | [inferred] |
