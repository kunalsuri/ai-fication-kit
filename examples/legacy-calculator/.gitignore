# Files generated by ai-fication-kit (shazam / install).
# These are intentionally git-ignored so the example stays in its
# "before" state. Run `node install.mjs uninstall .` to remove them.
CLAUDE.md
AGENTS.md
ai/
.claude/
